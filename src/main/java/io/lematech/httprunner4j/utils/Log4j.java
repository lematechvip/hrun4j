package io.lematech.httprunner4j.utils;

/**
 * 
* @Title: Log4j.java 
* @Package org.tspring.atfwa.util 
* @Description: 日志信息输出 
* @author tspring2014@gmail.com   
* @date 2014年4月25日 上午11:23:08 
* @version V1.0
 */

public class Log4j {
/*	private static Logger logger;

	private static String filePath = "config/log4j.properties";	

	private static boolean flag = false;

	public static synchronized void getPropertyFile() {
		logger = Logger.getLogger("Tspring4pc");		
		PropertyConfigurator.configure(new File(filePath).getAbsolutePath());
		flag = true;
	}

	private static void getFlag() {
		if (flag == false){
			Log4j.getPropertyFile();
		}
	}

	public static void logInfo(String message) {
		Log4j.getFlag();
		logger.info(message);		
	}

	public static void logError(String message) {
		Log4j.getFlag();
		logger.error(message);
	}

	public static void logWarn(String message) {
		Log4j.getFlag();
		logger.warn(message);
	}
	*/
	
}
